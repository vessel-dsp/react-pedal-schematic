# @vessel-dsp/simulation

Workspace-private simulation readiness, compile, and runtime adapter APIs for
Vessel DSP `CircuitDocument` values.

This package depends on `@vessel-dsp/core` only. It has no React dependency and
does not create browser audio globals from the root entrypoint.
